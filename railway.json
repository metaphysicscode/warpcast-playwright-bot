{
  "build": {
    "builder": "NIXPACKS",
    "nixpacksPlan": {
      "phases": {
        "install": {
          "cmds": ["pip install -r requirements.txt", "playwright install chromium"]
        },
        "start": {
          "cmd": "python app.py"
        }
      }
    }
  }
}
